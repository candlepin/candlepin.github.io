sudo: false
language: ruby
rvm:
- 2.3.4
branches:
  only:
    - master
addons:
  apt:
    packages:
    - graphviz
    - openjdk-8-jre-headless
script: ./_travis/build
bundler_args: --without development --deployment
